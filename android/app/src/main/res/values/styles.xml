<resources>

    <!-- Base application theme. -->
    <!-- Added package react-native-edge-to-edge to meet Android 15 and above requirement of edge-to-edge support -->
    <!-- As per that package recommendation, changed from Theme.AppCompat.DayNight.NoActionBar to Theme.EdgeToEdge -->
     <!-- Also see comments in js/components/KeyboardAvoidingDialog.js -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- BootTheme should inherit from Theme.SplashScreen -->
    <style name="BootTheme" parent="Theme.BootSplash">
       
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>